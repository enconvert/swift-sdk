/** Enconvert — Swift SDK for the Enconvert file conversion API. */

/// The SDK version — keep in sync with the release git tag at every release
/// (SwiftPM versions are git tags; there is no manifest field to read at runtime).
public let VERSION = "0.1.1"
